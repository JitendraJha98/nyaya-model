## What this changes

## Evidence

- [ ] `python -m pytest -q` passes locally
- [ ] If retrieval, prompting or the reader changed: predictions committed under `outputs/eval-v1/<label>/`
      and the paired comparison under `reports/eval_v1_comparison_<label>.json`
- [ ] If an act was added: coverage probe numbers before/after, and the ≥ 98%-clean check
- [ ] Docs updated where a number changed (README, `docs/RESULTS.md`, cards)

## Anything a reviewer should look at first
