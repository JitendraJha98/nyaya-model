# Core — retrieval, scoring, statute-DB build, CLI. No GPU stack; the retriever
# is standard-library Python.
#
#   GPU training + evaluation (Kaggle T4 tested):  pip install -r requirements-train.txt
#   or, from a clone:                              pip install -e ".[train,dense]"
#   pinned versions that passed the test suite:    requirements.lock
pyyaml>=6.0
requests>=2.32
huggingface_hub>=0.24
